# Student-Service-Portal-Backend
Built a backend module for a Student Service Portal that receives multiple service requests from students. The portal first organizes requests by urgency, then process emergency items immediately, maintain an audit record of all processed requests, support fast lookup of request IDs, and finally generate a hierarchical view of the processed data. 
